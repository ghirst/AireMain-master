namespace CalcNameSpace.HelloWorld
{
    public class CalculatorMain
    {
        public decimal CalculatorDivider(decimal value1, decimal value2)
        {
            var equalsSomething = (value1 / value2);

            return equalsSomething;
        } 
    }
}